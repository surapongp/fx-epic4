# SA-1278 — Acceptance Criteria

**เรื่อง:** การตรวจสอบหรือทำรายการ Forward Contract เมื่อระบบมีปัญหา และ/หรือรายการไม่ถูกส่งไป AS400
**Role:** IT / BU
**เป้าหมาย:** เห็นรายการจอง FC ทุกรายการ + สถานะการส่ง AS400, รู้ว่ารายการไหนค้างและค้างเพราะอะไร, และกู้รายการที่ค้างให้จบ end-to-end โดย **ไม่สร้างสัญญาซ้ำ**

---

## โมเดลสถานะ (อ้างอิง)

AS400 core API เป็น **synchronous — ตอบผลทันที** จึง **ไม่มีสถานะ "รอ ack"** รายการมี 2 สถานะพัก และการค้างเกิดที่รอยต่อ App → AS400 แยก 2 ชนิด:

| สถานะ / ชนิดค้าง | ความหมาย | การกู้ |
|---|---|---|
| `ออกสัญญาแล้ว (App)` | App เสร็จ · ยังไม่เข้า AS400 | ส่งเข้า AS400 |
| ค้าง — `error` | ระบบไม่รับ (refused/maintenance) · **ไม่ถึง core แน่นอน** | **ส่งใหม่ได้เลย** |
| ค้าง — `indoubt` | timeout · **ไม่รู้ว่า core commit แล้วหรือยัง** | **ตรวจสอบ (reconcile) ก่อน** ส่งซ้ำ |
| `เสร็จสมบูรณ์` | AS400 ตอบ success · เข้า core · ปิดรายการ | — |

---

## Acceptance Criteria

### AC-1 · แสดงรายการจองทั้งหมด + สถานะการส่ง AS400
- [ ] **กำหนดให้** เปิดแท็บ SA-1278 · **ต้อง** แสดงรายการจอง FC ทุกรายการในตาราง พร้อมคอลัมน์: Reference No., ลูกค้า, รายการ (ซื้อ/ขาย · จำนวน · สกุล), สถานะปัจจุบัน, สถานะ AS400, อัปเดตล่าสุด, ปุ่มจัดการ
- [ ] **ต้อง** แยกสีสถานะชัดเจน: ปกติ (ฟ้า), ค้าง-error (แดง), ค้าง-indoubt (เหลือง), เสร็จสมบูรณ์ (เขียว)
- [ ] รายการที่ค้าง **ต้อง** ไฮไลต์แถว (พื้นแดงอ่อน) + แสดงข้อความสาเหตุใต้ badge

### AC-2 · การ์ดสรุป (KPI)
- [ ] **ต้อง** แสดง 4 การ์ด: รายการทั้งหมด · ระหว่างดำเนินการ · ค้าง/มีปัญหา · เสร็จสมบูรณ์
- [ ] การ์ด "ค้าง/มีปัญหา" **ต้อง** แยกจำนวนย่อย: `ส่งไม่สำเร็จ (error) X · ไม่ทราบผล (indoubt) Y`
- [ ] **ต้อง** มีภาพรวมไปป์ไลน์แสดงจำนวนรายการต่อสถานะ + บรรทัดเตือนจำนวนรายการที่ค้างที่รอยต่อ App → AS400

### AC-3 · ค้นหา / กรอง
- [ ] **ต้อง** ค้นหาด้วย Reference / ชื่อบริษัท / custcode ได้ (พิมพ์แล้วกรองทันที คงตำแหน่งเคอร์เซอร์)
- [ ] **ต้อง** กรองตามสถานะ (dropdown) และ toggle "เฉพาะที่ค้าง" ได้
- [ ] **ต้อง** แสดงตัวนับ "แสดง N / ทั้งหมด M รายการ"

### AC-4 · ดูรายละเอียด + Timeline
- [ ] **เมื่อ** กด "ดู" · **ต้อง** เปิด modal แสดงข้อมูลรายการ + stepper สถานะ + **timeline** ทุกเหตุการณ์ (จอง → ส่ง/พยายามส่ง → ปิด) พร้อมเวลา/ผู้ทำ/ผล (สำเร็จ/ล้มเหลว)
- [ ] รายการที่ค้าง **ต้อง** แสดงกล่องเตือนบอกชนิดการค้าง + คำอธิบาย + ปุ่มกู้ที่เหมาะกับชนิดนั้น

### AC-5 · กู้รายการปกติ (ยังไม่ส่ง)
- [ ] **กำหนดให้** รายการสถานะ `ออกสัญญาแล้ว (App)` (ไม่ค้าง) · **เมื่อ** กด "ส่งเข้า AS400" · **ต้อง** เปลี่ยนเป็น `เสร็จสมบูรณ์`, ได้ AS400 Ref, และบันทึก timeline

### AC-6 · กู้รายการค้างแบบ `error` (ส่งใหม่)
- [ ] **กำหนดให้** รายการค้างชนิด `error` (เช่น connection refused / maintenance) · **เมื่อ** กด "ส่งใหม่" · **ต้อง** ส่งเข้า AS400 ได้เลย (ไม่ต้องตรวจสอบก่อน) → `เสร็จสมบูรณ์` + Ref + timeline

### AC-7 · กู้รายการค้างแบบ `indoubt` (ตรวจสอบก่อนส่งซ้ำ) — สำคัญสุด 🔑
- [ ] **กำหนดให้** รายการค้างชนิด `indoubt` (timeout) · ปุ่มที่เห็น **ต้อง** เป็น "ตรวจสอบ AS400" (ไม่ใช่ "ส่งใหม่")
- [ ] **เมื่อ** กด "ตรวจสอบ AS400" **และ** AS400 มีรายการนี้ใน core อยู่แล้ว · **ต้อง** ปิดรายการเป็น `เสร็จสมบูรณ์` **โดยไม่ส่งซ้ำ** และ timeline ระบุว่า "พบใน core → ปิดโดยไม่ส่งซ้ำ"
- [ ] **เมื่อ** กด "ตรวจสอบ AS400" **และ** AS400 ไม่มีรายการนี้ · **ต้อง** ส่งใหม่แล้วปิดรายการ พร้อม timeline ระบุว่า "ไม่พบ → ส่งใหม่"
- [ ] **ต้องไม่** มีทางใดที่รายการ `indoubt` ถูกส่งซ้ำโดยไม่ผ่านการตรวจสอบก่อน (กัน Forward Contract ซ้ำ)

### AC-8 · ดำเนินการรายการค้างทั้งหมด (bulk)
- [ ] **เมื่อ** กด "ดำเนินการรายการค้างทั้งหมด" · **ต้อง** แสดง confirm สรุปว่าจะ *ส่งใหม่* กี่รายการ (error) และ *ตรวจสอบก่อน* กี่รายการ (indoubt)
- [ ] **ต้อง** กู้ทุกรายการที่ค้างจนจบ โดย **รายการ `indoubt` ต้องวิ่งผ่าน reconcile เสมอ** (ห้าม resend ทื่อ)
- [ ] **ต้องไม่** แตะรายการที่ยังไม่ค้าง (เช่นรายการปกติที่รอส่ง) — bulk จัดการเฉพาะที่ค้าง
- [ ] **เมื่อ** ไม่มีรายการค้าง · **ต้อง** แจ้งเตือนและไม่ทำอะไร

### AC-9 · จำลองเหตุระบบขัดข้อง (สำหรับสาธิต/ทดสอบ)
- [ ] **เมื่อ** กด "จำลองเหตุระบบขัดข้อง" · **ต้อง** สุ่มทำให้รายการที่ยังไม่จบบางรายการค้าง (ชนิด error หรือ indoubt) พร้อมบันทึก timeline การพยายามส่งที่ล้มเหลว
- [ ] **เมื่อ** ไม่มีรายการที่ดำเนินการได้ · **ต้อง** แจ้งเตือนและไม่ทำอะไร

### AC-10 · ออกรายงานสถานะ
- [ ] **เมื่อ** กด "ออกรายงาน" · **ต้อง** ดาวน์โหลดไฟล์รายงานสถานะ end-to-end ทุกรายการ (สถานะ + สาเหตุค้าง + AS400 Ref) + สรุปยอด (เสร็จ/ระหว่างดำเนินการ/ค้าง แยก error·indoubt)

### AC-11 · รีเซ็ตข้อมูล
- [ ] **เมื่อ** กด "รีเซ็ตข้อมูล" และยืนยัน · **ต้อง** คืนข้อมูลตัวอย่างกลับค่าเริ่มต้น

---

## Non-functional / ขอบเขต
- [ ] ทุก action สะท้อนผลทันที (re-render) และคง filter/modal ที่เปิดอยู่
- [ ] เป็น customer self-service (จอง+ยืนยัน+ออกสัญญา atomic · ไม่มี draft) — ไม่มีสถานะ "รอ ack"
- **ขอบเขตโปรโตไทป์:** ข้อมูล mock ในไฟล์เดียว; ผลการ reconcile (พบ/ไม่พบใน core) ถูก seed ล่วงหน้า (`coreHasIt`) เพื่อสาธิต — **ของจริง** คือยิง inquiry API ไปถาม AS400
- **ข้อมูล seed สำหรับทดสอบ:** 9 รายการ = เสร็จสมบูรณ์ 2 · รอส่ง 3 · ค้าง 4 (error 2, indoubt 2: มี 1 รายการที่ core มีอยู่แล้ว → ทดสอบเคสกันสัญญาซ้ำ)

---

## Traceability → โค้ด ([sa-1278.js](sa-1278.js))
| AC | ฟังก์ชันหลัก |
|---|---|
| AC-1, AC-4 | `sa78Row` · `sa78StageBadge` · `sa78As400Cell` · `sa78Detail` · `sa78Stepper` |
| AC-2 | `sa78Stats` · `hSA1278` (cards + pipe) |
| AC-3 | `hSA1278` (toolbar) · `bindSA1278` |
| AC-5, AC-6 | `sa78Send` |
| AC-7 | `sa78Reconcile` (แยกทางด้วย `coreHasIt`) |
| AC-8 | `sa78RecoverAllStuck` → `sa78Recover` |
| AC-9 | `sa78SimOutage` |
| AC-10 | `sa78Export` |
| AC-11 | `bindSA1278` (`sa78-reset`) |
